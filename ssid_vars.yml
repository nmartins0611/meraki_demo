---
meraki_api_key: "{{ meraki_key }}"
org_id: "{{ org_id }}"

SSID:
  - name: 
    number: 1
    enabled: true
    authentication:
      auth_mode: psk
      encryption_mode: wpa
      psk: 
      wpa_encryption_mode: WPA2 only
    visible: false
    availableOnAllAps: true
    
  - name: 
    number: 2
    enabled: true
    authentication:
      auth_mode: psk
      encryption_mode: wpa
      psk: 
      wpa_encryption_mode: WPA2 only
    state: present
    visible: true
    availableOnAllAps: true
    visible: true
    availableOnAllAps: true
    
  - name: 
    number: 3
    enabled: false
    authentication:
      auth_mode: psk
      encryption_mode: wpa
      psk: 
      wpa_encryption_mode: WPA2 only
    state: present
    visible: false
    availableOnAllAps: true
    
  - name: 
    number: 4
    enabled: true
    authentication:
      auth_mode: psk
      encryption_mode: wpa
      psk: 
      wpa_encryption_mode: WPA2 only
    state: present
    visible: false
    availableOnAllAps: true

